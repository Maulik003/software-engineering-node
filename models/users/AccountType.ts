/**
 * @file Declares AccountType data type representing relationship between
 * users and type of account, as in user has personal account
 */

enum AccountType {
    Personal = "PERSONAL",
    Academic = "ACADEMIC",
    Professional = "PROFESSIONAL"
};

export default AccountType;