/**
 * @file Declares MaritalStatus data type representing relationship between
 * users and marital status, as in user is married
 */

enum MaritalStatus {
    Married = "MARRIED",
    Single = "SINGLE",
    Widowed = "WIDOWED"
};

export default MaritalStatus;